# Current status

## Local project finalization — 2026-09-06

Local implementation and verification are complete. The owner has **deferred Azure deployment and will not create a subscription**. Useful M3 code, infrastructure, workflows, tests and operating procedures are retained as preparation. **Azure deployment, effective cloud permissions, billing, cloud rollback and teardown are NOT RUN; original M3 cloud acceptance is incomplete.** No Azure account/resource, identity/settings, billing, registry publication or cloud workflow operation is part of finalization.

The implementation is committed and normally pushed to `main` in `https://github.com/dahornea/relaylab`: **`9610f62dd89a4e9c964dc016ee36b5c19743fa92` passed actual GitHub CI [33992623827](https://github.com/dahornea/relaylab/actions/runs/33992623827)**. It includes the 47 reviewed implementation/preparation files and local-only finalization documentation, starting from `0305c660cf2388d9fae8061bff715f02eb604c88`. This evidence-recording follow-up changes only docs/STATUS.md; it does not alter the tested implementation. Unrelated MongoDB/Redis containers and data remain untouched.

Prepared: separate bootstrap/main Terraform roots, Entra-authenticated state storage, GitHub environment OIDC, scoped workload identities, two SQL Basic databases, Standard Service Bus, Basic ACR, three Consumption Container Apps, two explicit schema jobs, Application Insights/Log Analytics, immutable-image deployment, authenticated ingress and receiver ACLs, cloud failure/telemetry verification, compatible-image rollback and two-stage teardown. [CLOUD.md](CLOUD.md) retains the optional inventory/cost/permission plan and runbook; it is not an active cloud approval request. Only the primary agent changed files/executed checks.

## Schema and security boundary

**M2 local setup still needs a fresh M2 schema and cannot upgrade M1. Cloud M3 needs new empty databases and its explicit version-1 baseline; it refuses unversioned M1 AND M2 databases without erasing data.** No existing user database was reset. SQL application locking serializes baseline creation. Version/database-kind/normalized model hash gates startup and schema jobs. Runtime identities receive table-scoped DML, with DDL confined to the schema job identity. Azure principal SID creation/effective grants remain unexecuted.

Application rollback re-deploys previously verified M3 image digests as new Single-mode revisions after their schema check. It preserves SQL, rejects incompatible/unversioned models and requires a distinct prior release. There is no M1/M2 adoption migration or database downgrade. Marker verification does not detect arbitrary administrator schema drift.

Production ingress requires single-tenant app-only JWT validation and exact object-ID ACLs; receiver workload and diagnostics identities are separate. HTTP stays outside SQL transactions, logical/work/attempt identities and M2 fencing remain intact. The public Azure-services SQL firewall boundary is explicit; this small demo has no private network isolation. Durable receiver deduplication remains conditional on retained SQL and does not establish universal exactly-once delivery.

## Executed local verification

- Terraform 1.16.1: both roots passed fmt and backend-disabled validate with locked AzureRM 5.4.0 / AzureAD 3.9.0 providers. Official provider checksum locks cover Windows/Linux amd64. No Azure plan ran.
- actionlint 1.7.12, downloaded with official SHA256 verification, passed both workflow files. All authored PowerShell scripts passed parsing.
- Focused checks: 15/15 passed (12 JWT/configuration/receiver tests plus 3 real SQL schema cases), followed by 13/13 security tests after adding the shipping Production route guard case. An initial synthetic JWT metadata configuration failure was corrected; real signature validation remains enabled.
- Complete `pwsh -NoProfile -File eng/verify-m3.ps1` **passed, exit 0**: locked NuGet restore, Release build with **zero warnings/errors**, **63/63 tests with zero failed/skipped**, fresh real SQL/broker infrastructure and Linux container recovery demo. Source Fact/InlineData inventory and TRX cases match: 33 real SQL/broker cases (the previous 30 plus 3 schema), 17 pure policy cases and 13 security/HTTP configuration cases. Host tests ran on Windows; the fresh demo built/ran all three applications on Linux amd64. Docker access needed a sandbox escalation; no external cloud action was involved.
- Final cloud-script review corrections did not change application source, tests, locks or container behavior. After both fixes, `pwsh -NoProfile -File eng/verify-m3.ps1 -StaticOnly` passed again; log `artifacts/m3/static-final.log`. Terraform's actual empty-state `show -json` shape was inspected locally. Azure command execution remains unverified. Later documentation-only edits record these results without invalidating runtime evidence.
- Logs/reports are under ignored `artifacts/m3`, `artifacts/verification`, `artifacts/test-results`. Test infrastructure is disposable and separate from existing user databases.

## Fresh recovery and telemetry evidence

- Isolated Compose project `relaylab-verify-a4caee3c60`: ordinary delivery `c997d46c-54e6-4ed7-ad54-72a6127b6288` returned 202, repeated submission 200, then Delivered with one attempt/effect.
- Recovery delivery `a449020a-0c12-4256-a949-54da68c7d939`: three 503 attempts exhausted, replay returned 202 then 200 with stable work identity. Actual worker SIGKILL followed a committed receiver effect while sender state remained Processing/Started. Worker/receiver restart retained SQL. Final state: **Delivered, generation 1, five attempts, one Interrupted/Unknown, one durable receiver effect**.
- Aspire actually ingested trace `fb1dbdfdecd8bedc671759a09018f927`, **23 spans**, all eight required lifecycle kinds and API/worker/receiver services. Evidence: `artifacts/verification/fresh-{demo-result,recovery-result,recovery-trace}.json` and exhausted/interrupted/recovered.json. Azure exporter/metric query preparation does not imply cloud ingestion. Local numeric metric UI evidence from unchanged M2 instruments remains historical; it was not rerun or misrepresented as an Azure metric check.
- Fresh candidate inputs were copied and hash-checked, including intended untracked files; manifest `artifacts/verification/candidate-inputs.sha256`. Test/demo-owned containers, volume, network and generated .env were removed. The only remaining running containers are the pre-existing unrelated MongoDB/Redis. Ignored logs, source copies, images and build caches remain local.

## Review, hygiene and remaining execution

Configured read-only researcher resolved SQL managed identity/SID setup, current pricing and exporter/query details using official sources, recorded in SOURCES/CLOUD/DECISIONS. The configured read-only reviewer inspected the complete tracked/untracked M3 candidate and verification evidence. Two concrete findings were fixed and the corrections reviewed: deployment now converges an empty/interrupted foundation from actual resource state rather than state-blob existence; cloud trace verification selects full correlated traces so child HTTP spans without delivery tags remain included. **No concrete blocker remains in the repository preparation.** Stale future-work wording was corrected. No further runtime source change was needed.

Reviewed all 47 intended changed/new files and ignore boundaries. No generated credential, private machine path, state/plan, build output, cache or test artifact is included in intended Git inputs or the Docker context. Synthetic JWT keys/tokens, placeholder UUIDs and existing emulator test values remain intentional. `git diff --check` passed. AGENTS, configured roles and LICENSE are unchanged. Finalization changes only documentation, a CI display name and cloud-workflow comments/display name; runtime code, tests and local demo behavior retain the valid 63-test evidence.

The configured read-only reviewer completed a focused finalization review of public claims and the actual workflow trigger/call chain: **no concrete blocker remains**. `eng/verify-m3.ps1 -StaticOnly` passed again; `artifacts/finalization/static.log` records it. Input hashes confirm no runtime/test/lock/local-demo changes since the full local run, so that acceptance was reused rather than redundantly repeated. Actual Linux CI subsequently executed the complete suite and demo against the committed implementation.

Ordinary repository use requires no Azure account/credentials. The inspected dependency chain is `ci.yml` → `eng/verify-m3.ps1` → `eng/verify.ps1` → `eng/demo.ps1`. Terraform only initializes with its backend disabled and validates configuration; cloud scripts are parsed without execution. `cloud.yml` has only `workflow_dispatch`, no automatic or reusable-workflow trigger, and no normal CI caller/dispatcher. Cloud workflow restrictions are validated statically without dispatching it. Any future cloud effort would require a new owner decision and external authorization; quotas/SKUs, tenant policy, role propagation, environment protections and Azure CLI behavior remain unverified.

## Executed implementation CI

- **Run:** [33992623827](https://github.com/dahornea/relaylab/actions/runs/33992623827), workflow `Repository verification`, event **push**, tested SHA **`9610f62dd89a4e9c964dc016ee36b5c19743fa92`**, conclusion **success**. Logs confirm **Ubuntu 24.04.4**, SDK **10.0.400**, Linux amd64 actionlint, Docker Linux execution and the unfiltered `eng/verify-m3.ps1` entry point. No Azure credential, login, backend access, plan or cloud deployment step ran.
- Both Terraform roots passed locked provider initialization with `-backend=false` and validation. Workflow linting and PowerShell parsing passed. Locked NuGet restore and Release build passed with **zero warnings/errors**.
- Downloaded TRX was inspected independently: **63/63 passed, zero failed/skipped**, with every expected class/case present: Acceptance 9, Connectivity 1, Failure 5, Retry 4, Recovery 8, BrokerRecovery 2, Telemetry 1, SchemaDeployment 3, CloudSecurity 13, Policy 6, RetryPolicy 11. This includes **33 real SQL/broker integration cases**, 13 security/HTTP configuration cases and 17 pure policy cases.
- Dependency logs confirm SQL Server **2022 CU14 (16.0.4135.4)** ready for connections and Service Bus emulator launching on **CBL-Mariner/Linux X64** with deliveries queue, PT1M PeekLock and MaxDeliveryCount 10. Emulator queue-size diagnostics appeared during fault tests; all observable acceptance oracles passed. These are local emulator results, not evidence of Azure broker behavior.
- Fresh project `relaylab-verify-5ba64842a4` built the three application images. Ordinary delivery `35834106-6b9d-463b-bb76-0c9a545adbfb` reached Delivered with one attempt/effect and repeated submission 200. Recovery `d97489fa-81ad-4f89-a46b-efc1f4dbc578` exhausted three 503 attempts, returned replay 202/200, and underwent actual worker SIGKILL after receiver effect while sender remained Processing/Started. Final history: **Delivered, generation 1, five attempts, Interrupted/Unknown, one receiver effect**.
- Aspire ingested trace `07536432cb8311f7c3ea51384085470c`: **24 spans**, all eight required lifecycle kinds, all three services and one correlated trace identity. Exhausted/interrupted/recovered JSON, test results and cleanup logs were inspected. Owned demo teardown passed.
- The committed cloud workflow was read back from GitHub and matched the statically reviewed manual-only source. Only ordinary repository verification ran for the implementation push. `cloud.yml` has no push, PR, schedule, workflow_run or workflow_call trigger; normal CI neither calls nor dispatches it. No cloud workflow was dispatched to test that restriction.
- Inspection commands: `gh run view 33992623827 --repo dahornea/relaylab --log` and `gh run download 33992623827 --repo dahornea/relaylab --name m3-verification --dir artifacts/finalization/ci-implementation`. Metadata, logs, downloaded reports and the checked summary are under `artifacts/finalization/ci-implementation*`. Remote artifacts have seven-day retention; this record and run URL retain the compact evidence. No CI fix or test weakening was needed.

## Historical M2 evidence (unchanged baseline only)

- Implementation `4b43be543768fc0520fd4cc445d1e1442b253d72`: [CI 33988334119](https://github.com/dahornea/relaylab/actions/runs/33988334119) passed Ubuntu 24.04.4 / SDK 10.0.400, locked restore, zero-warning Release build, 47/47 tests (30 SQL/broker and 17 pure), fresh Linux recovery demo with five attempts, Unknown interruption and one effect, and correlated Aspire spans.
- Documentation `0305c660cf2388d9fae8061bff715f02eb604c88`: [CI 33988793932](https://github.com/dahornea/relaylab/actions/runs/33988793932) also passed the 47 cases and recovery demo, as finalized by the owner. These results do not establish M3 CI or Azure execution.
- Local host/tool baseline: Windows 11, SDK 10.0.400 / runtime 10.0.11, PowerShell 7.6.5, Docker Desktop 4.89.0 with Linux Engine 29.7.2 and Compose 5.5.0. SQL 2022-CU14-ubuntu-22.04, Service Bus emulator 2.0.0, Aspire 13.5.2; EF SQL Server 10.0.11, Service Bus SDK 7.20.2, OpenTelemetry 1.18.0, Testcontainers 4.14.0, xUnit 2.9.3 / VSTest 18.9.0. M3 adds Azure.Identity 1.21.0, Azure Monitor exporter 1.9.0 and JwtBearer 10.0.11; lock files are updated.
